<%= form_for @tip, :html => {:multipart => true} do |f| %>
  <% if @tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip.errors.count, "error") %> prohibited this tip from being saved:</h2>

      <ul>
      <% @tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
   <%= f.label "Kategori" %><br />
 <%= f.select :categorie_id, Category.all.collect {|category| [category.categoryname, category.id]}  %>
  </div>
  <div class="field">
    <%= f.label "Bild" %><br />
    <%= f.file_field :image %>
  </div>
   <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => session[:user_id] %>
  </div>
  <div class="actions">
    <%= f.submit :value => "Lägg till tips" %>
  </div>
<% end %>
